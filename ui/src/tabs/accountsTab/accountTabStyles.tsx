import {makeStyles, shorthands} from "@fluentui/react-components";

export const accountTabStyles = makeStyles({
    tableWrapper: {
        width: '800px',
        ...shorthands.padding('20px', '10px', '10px', '0px'),
    }
});